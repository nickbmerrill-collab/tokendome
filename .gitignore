node_modules/
.vercel/
dist/
*.tgz
# Tracked: the bundled SDK tarballs Vercel ships from /public are
# regenerated by CI on every push to main and need to live in git.
!public/*.tgz
.env*
.DS_Store
.vercel
.audit-output.log
.env.production
